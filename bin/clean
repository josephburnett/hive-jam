#!/usr/bin/env bash

set -e

ROOT=`git rev-parse --show-toplevel`

cd $ROOT

( cd hive-jam ; $ROOT/bin/vendor/lein clean )

if [ -f build/hive-jam ]; then
    rm build/hive-jam
fi

if [ -f osc-bridge/src/hivejam/data/data.go ]; then
    rm osc-bridge/src/hivejam/data/data.go
fi
